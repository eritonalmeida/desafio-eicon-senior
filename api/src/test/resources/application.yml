spring:
    main:
        banner-mode: off
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:test_db
        username: sa
        password:
topicos:
  creditos:
    request:
      topic: "creditos.request.topic.v1"